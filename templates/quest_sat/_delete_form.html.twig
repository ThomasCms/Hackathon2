<form method="post" action="{{ path('quest_sat_delete', {'id': quest_sat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quest_sat.id) }}">
    <button class="btn">Delete</button>
</form>
